##############################################################################
# This file contains build specific configuration data. If you have build properties
# that are common to all your builds use shared.ant.properties

#
# Build specific properties can be put in sandbox.ant.properties, or a 
# copy of this file. To run a build other than 'sandbox' call ant with -DTYPE=xxxx
##############################################################################

# For JSP Compiling At The Module Level (Must set jspc.container.libs)
# ie... based on a default JBoss 4.x deployment (using Tomcat 5.5)
#    jboss.config=default
#    container.home=${env.JBOSS_HOME}
#    jspc.container.libs=${container.home}/server/${jboss.config}/deploy/jbossweb-tomcat55.sar;${container.home}/server/${jboss.config}/lib
jspc.container.libs=C:/jb405ga/server/default/deploy/jbossweb-tomcat55.sar;c:/jb405ga/server/default/lib
